class UnseenModel {

  // Field ==ตัวแปร
     String name, detail, urlPicture, code;
     double lat, lng;


  // Constructor ชื่อเดียวกับ class
  UnseenModel(this.name, this.detail, this.urlPicture, this.code, this.lat, this.lng);

  
}